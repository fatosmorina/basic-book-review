<%= simple_form_for @book, :html => { :multipart => true } do |form| %>
	<%= form.input :category_id, collection: @categories, prompt: "Select a category" %>
	<%= form.input :title, label: "Title" %>
	<%= form.input :description %>
	<%= form.input :author %>
	<%= form.button :submit %>
<% end  %>